# platojobs_nrf_mesh (Discontinued)

This package is **discontinued**.

Please migrate to:

- `nrf_mesh_flutter`: https://pub.dev/packages/nrf_mesh_flutter

## Why

The original plugin has been renamed and continues to be maintained under the `nrf_mesh_flutter` package name.

## Migration

Replace your dependency:

```yaml
dependencies:
  nrf_mesh_flutter: ^1.0.2
```

Update imports:

```dart
import 'package:nrf_mesh_flutter/nrf_mesh_flutter.dart';
```

